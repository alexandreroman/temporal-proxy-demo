# Keep the build context down to what the image actually compiles.
.git
.claude
.env*

# Not part of the build, and the one exclusion that is about more than
# size: the Temporal Cloud client key lives in k8s/certs/, and the
# runtime image never copies it in.
k8s/
*.md
Makefile
LICENSE
.casper.json
.DS_Store
