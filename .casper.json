{
  "workspace": {
    "copyFiles": [".env", "client.pem", "client.key"],
    "scripts": {
      "setup": "make worktree-init PORT=$CASPER_PORT GATEWAY_PORT=$((CASPER_PORT + 1)) UI_PORT=$((CASPER_PORT + 2))",
      "run": "make app-up",
      "teardown": "make app-down"
    }
  }
}
