# The operator's defaults suit this demo. In particular
# defaultVaultConnection.enabled already defaults to false, so there is
# nothing to override here: the VaultConnection under k8s/base/vault-secrets
# carries the Vault address instead, next to the resources that use it.
