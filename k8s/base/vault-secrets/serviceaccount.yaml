# The identity the operator presents to Vault. The Vault role created by
# k8s/vault/bootstrap.sh is bound to exactly this name and namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: temporal-proxy-vault
  namespace: temporal-proxy
